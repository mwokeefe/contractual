[project]
name = "contractual"
version = "0.1.0"
description = "Design by contract in vanilla python."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=7.4.4",
    "ruff>=0.15.7",
]
